spring:
  application:
    name: eureka
server:
  port: 8761
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    #高可用注册中心相关配置。单个注册中心配置false即可
    fetch-registry: false
    register-with-eureka: false