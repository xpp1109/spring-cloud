spring:
  application:
    name: client1
  cloud:
    config:
      uri: http://localhost:8000
  rabbitmq:
    host: localhost
    port: 5672
    password: guest
    username: guest
server:
  port: 7000
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka